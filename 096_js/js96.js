let fish = ['angel', 'clown', 'mandarin', 'sturgeon'];

// ลบ element ทั้งหมดใน array โดยเริ่มจาก index 0 และลบความยาวของ array ทั้งหมด
fish.splice(0, fish.length);

console.log(fish);  // ผลลัพธ์: []
