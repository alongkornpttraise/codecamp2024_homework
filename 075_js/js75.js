// "let i = 0;
// while (i != 10) {
//   i += 0.2;
// }
// "	


//โค้ดที่ให้มาจะทำงานไม่ปกติและอาจทำให้เกิดลูปที่ไม่มีที่สิ้นสุดเนื่องจากข้อผิดพลาดในการคำนวณทศนิยม.					


// แก้ไขโดย

let i = 0;
while (i < 10) {  // ตรวจสอบเงื่อนไขแบบนี้แทน
  i += 0.2;
}

console.log(i); // Output: 10.000000000000002 (ตัวเลขไม่แม่นยำ)
